# UniGame Static ECS Editor

Editor-only workflow and migration tooling for UniGame Static ECS packages.

This package should provide menus, diagnostics, authoring validation, and migration helpers.
Runtime code must stay in `com.unigame.staticecs` or `com.unigame.staticecs.unity`.
